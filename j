<img
                    src={`${backendUrl}${project.final_drawing}`}
                    alt="Preview"
                    className={`${
                      viewMode === "list"
                        ? "w-48 h-32 object-cover"
                        : "w-full h-40 object-cover"
                    }`}
                  />